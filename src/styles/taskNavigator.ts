import { StyleSheet } from "react-native";
import global from "./global";

export const activeColor = '#000000';

const style = StyleSheet.create({
  labelStyle: {
    fontFamily: global.fontFamily,
    fontSize: 20
  }
})

export default style;
